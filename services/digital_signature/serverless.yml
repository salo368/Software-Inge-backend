service: cdts-digital-signature
frameworkVersion: '4'

provider:
  name: aws
  runtime: python3.12
  region: ${opt:region, 'us-east-1'}
  stage: ${opt:stage, 'dev'}
  memorySize: 256
  timeout: 30
  architecture: arm64
  logRetentionInDays: 14
  environment:
    STAGE: ${self:provider.stage}
    SERVICE: ${self:service}

package:
  individually: true
  patterns:
    - '!**'
    - 'src/**/handler.py'

functions:
  sign: ${file(src/sign/function.yml)}
