# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# --- Virtual envs ---
.venv/
venv/
env/

# --- Node ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Serverless Framework ---
.serverless/
.serverless_plugins/

# Mirror of backend/libs/ copied into each service by functions.js at package time.
# Source of truth is backend/libs/. Copies must never be committed.
backend/services/*/libs/
backend/platform/*/libs/

# --- AWS / credenciales ---
.aws/
*.pem
access.csv

# scripts/e2e-signature.py drops runtime artifacts at the repo root
# so bash `echo > e2e-otp.txt` from a separate Shell invocation lands
# at the same path python resolves via --otp-file (git-bash MSYS
# rewrites `/tmp/...` differently per invocation, hence repo-local).
e2e-otp.txt
e2e-signature.log
credentials.csv
.env
.env.*
!.env.example

# --- Reportes de tests ---
tests/report.html
tests/report/
**/report.html

# --- IDE ---
.vscode/*
!.vscode/settings.json.example
!.vscode/extensions.json.example
.idea/
*.iml

# --- OS ---
.DS_Store
Thumbs.db
desktop.ini

# --- Build artifacts ---
dist/
build/
*.zip

.legacy_tmp/
