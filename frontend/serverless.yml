service: cdts-frontend
frameworkVersion: '3'

provider:
  name: aws
  region: ${opt:region, 'us-east-1'}
  stage: ${opt:stage, 'dev'}

custom:
  bucketName: cdts-frontend-${self:provider.stage}-${aws:accountId}

resources:
  Resources:
    FrontendBucket:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: ${self:custom.bucketName}
        WebsiteConfiguration:
          IndexDocument: index.html
          ErrorDocument: index.html
        PublicAccessBlockConfiguration:
          BlockPublicAcls: false
          BlockPublicPolicy: false
          IgnorePublicAcls: false
          RestrictPublicBuckets: false
        OwnershipControls:
          Rules:
            - ObjectOwnership: BucketOwnerPreferred

    FrontendBucketPolicy:
      Type: AWS::S3::BucketPolicy
      Properties:
        Bucket:
          Ref: FrontendBucket
        PolicyDocument:
          Statement:
            - Effect: Allow
              Principal: "*"
              Action: s3:GetObject
              Resource:
                Fn::Sub: "${FrontendBucket.Arn}/*"

    # HTTPS: el endpoint website de S3 es solo HTTP y los navegadores modernos lo bloquean
    FrontendCdn:
      Type: AWS::CloudFront::Distribution
      Properties:
        DistributionConfig:
          Enabled: true
          Comment: Front comercio CDTs (HTTPS)
          PriceClass: PriceClass_100
          HttpVersion: http2
          Origins:
            - Id: s3-website
              DomainName: ${self:custom.bucketName}.s3-website-${self:provider.region}.amazonaws.com
              CustomOriginConfig:
                OriginProtocolPolicy: http-only
          DefaultCacheBehavior:
            TargetOriginId: s3-website
            ViewerProtocolPolicy: redirect-to-https
            CachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6 # CachingOptimized
            Compress: true
          CustomErrorResponses:
            - ErrorCode: 404
              ResponseCode: 200
              ResponsePagePath: /index.html
              ErrorCachingMinTTL: 0
          ViewerCertificate:
            CloudFrontDefaultCertificate: true

  Outputs:
    WebsiteURL:
      Description: "URL publica del SPA"
      Value:
        Fn::GetAtt: [FrontendBucket, WebsiteURL]
    BucketName:
      Description: "Nombre del bucket S3"
      Value:
        Ref: FrontendBucket
    CloudFrontDomain:
      Description: "Dominio HTTPS del front del comercio"
      Value:
        Fn::GetAtt: [FrontendCdn, DomainName]
    CloudFrontId:
      Description: "Id de la distribucion (para invalidaciones)"
      Value:
        Ref: FrontendCdn
