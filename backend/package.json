{
  "name": "cdts",
  "version": "0.1.0",
  "private": true,
  "description": "CDTS - Serverless multi-servicio en AWS (Lambda + API Gateway + S3/CloudFront)",
  "engines": {
    "node": ">=20 <21"
  },
  "scripts": {
    "deploy:dev": "serverless-compose deploy --stage dev",
    "deploy:pro": "serverless-compose deploy --stage pro",
    "remove:dev": "serverless-compose remove --stage dev",
    "remove:pro": "serverless-compose remove --stage pro",
    "package": "serverless-compose package",
    "test": "python -m pytest tests --html=tests/report.html --self-contained-html"
  },
  "devDependencies": {
    "@serverless/compose": "1.3.0",
    "js-yaml": "4.1.0",
    "serverless": "3.38.0",
    "serverless-python-requirements": "6.1.1"
  }
}
