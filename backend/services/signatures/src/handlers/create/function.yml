description: >
  Opens a signature ceremony from a presigned PDF URL. M2M via
  X-Service-Key. Persists the row and returns sign_id + sign_url.
timeout: 20
events:
  - httpApi:
      method: POST
      path: /signatures
