description: >
  Returns the full state of a signature ceremony to the microfront.
  Public via sign_id (link-based auth); includes a fresh presigned GET
  URL for the original PDF.
timeout: 10
events:
  - httpApi:
      method: GET
      path: /signatures/{sign_id}
