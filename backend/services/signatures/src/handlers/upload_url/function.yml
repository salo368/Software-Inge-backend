description: >
  Issues a presigned PUT URL for evidence uploads (cedula front/back,
  face, signature drawing) directly to signatures' bucket. Auth by
  sign_id; presign TTL is 5 minutes.
timeout: 10
events:
  - httpApi:
      method: POST
      path: /signatures/{sign_id}/upload-url
